#!/usr/bin/env bash

cd $1
echo "Bundling $1/$2 into $3"

#TODO: Refactor out all these hardcoded node modules paths
NODE_PATH=$NODE_PATH:"./node_modules/":"$(pwd)/../libs/" ./node_modules/browserify/bin/cmd.js -u=z3javascript -u=process $2 --outfile $3
